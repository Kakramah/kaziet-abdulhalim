# تهيئة صفة المدير وتطبيق ترقيع الصلاحيات

هذا الملف يشرح كيف تسدّ الثغرة الموثّقة في PR الترقيع: السياسات القديمة كانت
تسمح لأي `auth.role() = 'authenticated'` (أي مستخدم مسجّل، وحتى مستخدم
"تسجيل مجهول" إن كان مفعّلاً) بالكتابة على `site_content` وصور `storage.objects`،
بينما المفترض أن يملك هذا الحق المدير وحده.

الحل: صفة `app_metadata.kaziet_admin = true` لا يستطيع أي مستخدم تعيينها
بنفسه (لا عبر `updateUser` من العميل، ولا من أي مكان غير SQL Editor أو
Admin API بصلاحية service role/postgres من جهة الخادم).

## حالة التطبيق على المشروع الحي «كازيه»

**الترقيع طُبِّق فعلياً على الإنتاج.** دُمج PR الترقيع (commit
`93daf336` على `main`)، وبعد إذن المالك: عيّن **مساعد المالك** (جلسة
Claude أخرى تعمل له، عبر موصل Supabase المباشر — لا المالك بنفسه يدوياً
ولا هذه الجلسة) `app_metadata.kaziet_admin = true` لحساب Auth الوحيد في
المشروع، ثم طُبِّق ملف `migrations/2026-09-25_restrict_admin_write_access.sql`
من `93daf336` نفسه عبر Supabase `apply_migration` بنتيجة `success=true`.

**ما تحقّقنا منه فعلياً بأنفسنا (من هذه الجلسة التوثيقية):**
- الـ commit المُطبَّق (`93daf336`) موجود فعلاً على `main` ومحتواه
  مطابق لما دُمج في PR — تحقّقنا بـ `git show`/`git log` على المستودع،
  لا بمعلومة منقولة.
- ما اختبرناه محلياً على Postgres 16 قبل الدمج (الأقسام أدناه) يبقى
  صحيحاً كوصف لمنطق SQL نفسه، لا كدليل على تنفيذه على الإنتاج.

**نتائج فحص `pg_policies` الفعلي بعد التطبيق، منقولة من مساعد المالك
الذي أجرى الفحص — لا نتيجة تحقّقنا بها بأنفسنا:**
**مساعد المالك** (جلسة Claude أخرى، لا المالك بنفسه يدوياً) أجرى فحص
`pg_policies` فعلياً على القاعدة الحيّة عبر موصل Supabase المباشر بعد
إذن المالك، ونقل نتيجته إلى جلسة Claude هذه حرفياً: **7 سياسات
بالضبط**، مطابقة تماماً لما توقّعه ملف الترقيع:

| tablename | policyname | cmd | ملاحظة |
|---|---|---|---|
| site_content | Allow public read | SELECT | قراءة عامة، لم تُمسّ |
| site_content | Admin write insert | INSERT | `TO authenticated WITH CHECK kaziet_is_admin()` |
| site_content | Admin write update | UPDATE | `TO authenticated USING/WITH CHECK kaziet_is_admin()` |
| storage.objects | Public Image Access | SELECT | قراءة عامة لـ`media`، لم تُمسّ |
| storage.objects | Admin Image Upload | INSERT | `TO authenticated`، `media AND kaziet_is_admin()` |
| storage.objects | Admin Image Update | UPDATE | نفس الشرط |
| storage.objects | Admin Image Delete | DELETE | نفس الشرط |

أي: **لا توجد أي سياسة `DELETE` على `site_content`**، و**لا توجد أي
سياسة كتابة متبقية تعتمد على `auth.role() = 'authenticated'` وحده بلا
شرط `kaziet_admin`**. كذلك نقل إلينا مساعد المالك نتيجة استعلام مستقل
بعد تعيين الصفة: `user_count = 1` و`admin_count = 1` — مستخدم Auth واحد
في المشروع، وهو نفسه يحمل صفة المدير الآن.

**هام لفهم طبيعة هذا التوثيق**: هذه نتائج **تحقّق قراءة فقط أجراه
مساعد المالك (جلسة Claude أخرى تعمل له) عبر موصل Supabase المباشر،
بعد إذن المالك، ونقلها إلى جلسة Claude هذه نصاً**. لم تُشغَّل هذه
الاستعلامات من هذه الجلسة ولا عبرها، ولم تتحقق هذه الجلسة منها بشكل
مستقل، لأنها **لا تملك أي وصول إلى مشروع Supabase الحي** (لا MCP ولا
اتصال مباشر بقاعدة البيانات) ولا حتى إلى الموقع الحي عبر الشبكة
(محاولة فتح `admin.html` على GitHub Pages فشلت بحجب صريح
`EGRESS_BLOCKED` من بروكسي شبكة هذه البيئة على نطاق
`kakramah.github.io`). فالفرق الدقيق: **السياسات فُحصت فعلياً وليس
"لم تُفحص إطلاقاً"** — لكن الفحص أجراه مساعد المالك عبر جلسة Claude
منفصلة، لا هذه الجلسة ولا المالك بنفسه يدوياً، وما تملكه هذه الوثيقة
هو نقل أمين لنتيجته، لا تحقّقاً مستقلاً من مصدر ثانٍ.

**ما بقي غير مختبَر فعلياً رغم ذلك (لا فحص قراءة يغطّيه):**
- **اختبار دخول المالك الفعلي من `admin.html` وتعديل حالة الوقود/رفع
  صورة كمدير حقيقي عبر الواجهة** — فحص `pg_policies` يثبت أن السياسات
  صحيحة في القاعدة، لا أن تدفّق التطبيق الكامل (تسجيل الدخول، الجلسة،
  JWT المُصدَر، طلب PostgREST الفعلي) يعمل تحديداً من طرف المستخدم.
- **حالة الجلسة/JWT في متصفح المالك تحديداً**: كما هو موضح في القسم 3،
  إن كان المالك مسجّلاً دخوله من قبل تعيين `kaziet_admin`، فجلسته
  الحالية قد تحمل JWT قديماً بلا الصفة حتى يسجّل خروجاً ودخولاً أو
  يجدّد الجلسة — هذا غير مؤكَّد من نتيجة `pg_policies` وحدها.

**الخطوة المتبقية الوحيدة لإغلاق التحقق بالكامل:** سجّل المالك دخوله
فعلياً من `admin.html` (بعد تجديد الجلسة إن احتاج الأمر، القسم 3)،
وجرّب تحديث حالة الوقود ورفع صورة فعلياً من الواجهة.

**ما اختُبر محلياً قبل الدمج وما بقي غير متحقق منه (تاريخي، قبل التطبيق
على الإنتاج):** قبل فتح PR الترقيع، شُغِّلت
ملفات SQL هذه (`supabase_setup.sql`، `fix_admin.sql`، وملف الترقيع)
فعلياً على نسخة Postgres 16 محلية معزولة (لا Supabase، ولا اتصال
بالإنترنت، ولا لمس أي قاعدة حيّة)، مقابل مخطط مصغّر يحاكي `auth.users`
و`auth.jwt()` و`storage.objects`، مع تفعيل RLS حقيقي ومحاكاة أدوار
`anon`/`authenticated` عبر `SET ROLE` و`SET request.jwt.claims` (لا
كاستعلام postgres مباشر). ثبت فعلياً: نجاح مسار مشروع جديد ومشروع قديم
بسياساته السابقة، توقف حارس "لا مدير" بـ `ROLLBACK` نظيف بلا تغيير
جزئي، ورفض/قبول الكتابة تبعاً للدور فعلياً (`UPDATE 0` لمستخدم عادي،
`UPDATE 1`/`INSERT 1` للمدير)، وتكرار تشغيل الترقيع (idempotency).
هذا تحقّق حقيقي من منطق SQL وRLS، لا مجرّد قراءة بصرية للبنية.

بعد مراجعة كشفت سياسة `Allow admin delete` الإضافية على القاعدة الحيّة
(القسم التالي)، أُعيد الاختبار محلياً على Postgres 16 من جديد بنفس
الطريقة، بمحاكاة الوضع الحي تحديداً: 8 سياسات قبل التطبيق (مطابقة لما
كشفه فحص `pg_policies` الحي حرفياً، بما فيها `Allow admin delete`
ومستخدم Auth واحد بلا أي مدير)، تأكّد فيها فشل الحارس أولاً بلا تغيير
(يطابق حالة مشروعكم وقتها، قبل تعيين المدير — تغيّر هذا لاحقاً، انظر
"حالة التطبيق على المشروع الحي" أعلاه)، ثم — بعد إضافة مدير اختباري
محلي وهمي فقط لإكمال المسار — نجاح تطبيق الترقيع، وسقوط `Allow admin
delete` فعلياً بلا سياسة `DELETE` بديلة على `site_content` (7 سياسات
بعدها)، ورفض `DELETE` فعلياً بالمحاكاة الثلاثية (مجهول/مستخدم عادي/مدير
— الثلاثة `DELETE 0`)، مع بقاء `UPDATE` يعمل للمدير (`UPDATE 1`).

ما بقي **غير متحقق** لأنه يحتاج مشروع Supabase حيّاً تحديداً: سلوك
GoTrue/PostgREST الفعلي على نسخة Supabase المُستضافة، وإعداد "تسجيل
مجهول" الفعلي في مشروعكم.

## انحراف مكتشف قبل الدمج على القاعدة الحيّة (قراءة فقط وقتها، بلا تعديل)

القسم التالي وصف اكتشافاً حصل **قبل** دمج PR الترقيع وتطبيقه، حين كانت
المراجعة قراءة فقط بلا أي تعديل. الوضع الحالي بعد الدمج والتطبيق
موصوف أعلاه في "حالة التطبيق على المشروع الحي": فحص `pg_policies`
الذي أجراه مساعد المالك بعد التطبيق يؤكد أن `Allow admin delete` سقطت
فعلاً ولا توجد أي سياسة `DELETE` بديلة على `site_content` — لكن هذا
تأكيد منقول من فحص مساعد المالك عبر موصل Supabase المباشر، لا تحقق
مستقل من هذه الجلسة (لا وصول Supabase لديها، كما هو موضح أعلاه). النص
الأصلي:

فحص `pg_policies` مباشرةً على مشروع Supabase الحي (قراءة فقط، لم يُعدَّل
أو يُطبَّق أي شيء وقتها) كشف ما لم يكن ظاهراً من ملفات المستودع وحدها:

- سياسة إضافية على `public.site_content`: **`"Allow admin delete"`**
  (`DELETE`، بشرط `auth.role() = 'authenticated'`) — **لم تكن موجودة في
  أي ملف SQL متتبَّع هنا** (لا `supabase_setup.sql` ولا `fix_admin.sql`
  التاريخيان أنشآها). أي مستخدم مسجّل دخوله كان يستطيع حذف صفوف
  `site_content` كاملة (`fuel_status`، `hero_image`) قبل هذا الإصلاح —
  وهذا أخطر من التعديل: حذف الصف الافتراضي يكسر الموقع لا يغيّره فقط.
  ملف الترقيع في هذا الـ PR يُسقطها الآن صراحة (القسم 1)، **بلا سياسة
  DELETE بديلة**: الواجهة لا تحتاج حذف صفوف إطلاقاً (تُفرِّغ القيمة بدل
  الحذف)، فتبقى `DELETE` على `site_content` ممنوعة على الجميع بلا
  استثناء — بما في ذلك المدير — لغياب أي سياسة تسمح بها.
- **عدد مستخدمي Auth في المشروع وقت هذا الاكتشاف: مستخدم واحد فقط، وصفر
  منهم يحمل `app_metadata.kaziet_admin = true`.** كان هذا يعني وقتها أن
  ملف الترقيع سيرفض التنفيذ فوراً (الحارس في القسم 2) حتى تُشغَّل خطوة
  تعيين المدير في القسم 1 أولاً — وهذا ما حصل فعلاً لاحقاً (انظر "حالة
  التطبيق على المشروع الحي" أعلاه: `user_count=1`، `admin_count=1`
  الآن). لم نقرأ بريد هذا المستخدم ولم نعدّل `app_metadata` أو أي
  بيانات في هذه المراجعة — كان ذلك قراراً خصّ المالك حصراً.
- هذا الانحراف يعني عملياً أن **قائمة السياسات المتوقعة أدناه (قسم 4.ج)
  ليست ضماناً مطلقاً لما هو موجود فعلياً على مشروعكم**: قد توجد سياسات
  أخرى أُضيفت يدوياً بعد كتابة ملفات المستودع الأصلية ولم يرصدها أحد
  بعد. **شغّلوا استعلام `pg_policies` (القسم 4.ج) على مشروعكم فعلياً قبل
  الاعتماد على أي قائمة مكتوبة هنا، وبعد التطبيق للتأكد من النتيجة.**

## 1. تعيين المدير الحقيقي (قبل تشغيل ملف الترقيع)

1. أنشئ حساب المدير إن لم يكن موجوداً (Authentication → Users → Add user)، أو استخدم حساباً موجوداً بالفعل يسجّل به المدير الدخول من `admin.html`.
2. اضبط `kaziet_admin` بإحدى طريقتين، وكلتاهما من جهة الخادم حصراً — لا من كود العميل أبداً:
   - **SQL Editor** في Supabase (استبدل البريد ببريد المدير الفعلي):

     ```sql
     UPDATE auth.users
     SET raw_app_meta_data =
       coalesce(raw_app_meta_data, '{}'::jsonb) || '{"kaziet_admin": true}'::jsonb
     WHERE email = 'admin@example.com';
     ```

   - أو **Admin API** الموثوق (سكربت خادم يحمل `service_role` key، لا يُبنى داخل `admin.html` ولا أي ملف يصل المتصفح):

     ```js
     await supabaseAdmin.auth.admin.updateUserById(userId, {
       app_metadata: { kaziet_admin: true }
     });
     ```

     الطريقتان متكافئتان أمنياً؛ المهم أن التنفيذ يبقى بصلاحية service role/postgres على الخادم، لا بمفتاح anon العام الموجود في `config.js`.
3. تحقّق:

```sql
SELECT email, raw_app_meta_data ->> 'kaziet_admin' AS is_admin
FROM auth.users
WHERE email = 'admin@example.com';
```

يجب أن يعيد `true`.

## 2. تطبيق ملف الترقيع

بعد التأكد من وجود مدير واحد على الأقل بصفة `kaziet_admin=true`:

- **مشروع قائم بالفعل (السياسات القديمة مُطبَّقة):** شغّل
  `migrations/2026-09-25_restrict_admin_write_access.sql` في SQL Editor.
  الملف يحتوي حارساً يوقف التنفيذ برسالة عربية إن لم يجد أي مدير مُعيَّن،
  فلن يقفل الصلاحيات على الجميع بالخطأ.
- **مشروع جديد من الصفر:** `supabase_setup.sql` أصبح يحتوي السياسات الآمنة
  مباشرة، فلا حاجة لملف الترقيع بعده. شغّله ثم عيّن المدير كما في الخطوة 1.
- `fix_admin.sql` لم يعد يفتح أي صلاحية؛ يبقى فقط لإصلاحات غير أمنية
  (صف `hero_image` الافتراضي ومُشغّل `updated_at`).

## 3. تحديث JWT بعد التعيين

`app_metadata` يُقرأ من الـ JWT الحالي للجلسة، لا من قاعدة البيانات مباشرة
في كل طلب. إن كان المدير مسجّلاً دخوله وقت تنفيذ الخطوة 1، يجب أن يُصدَر له
JWT جديد يحمل الصفة:

- الأبسط: يسجّل المدير **خروجاً ثم دخولاً** من جديد في `admin.html`.
- أو يستدعي العميل `client.auth.refreshSession()` إن كان مسجّلاً بالفعل ولا يريد تسجيل الخروج.

بدون هذه الخطوة، محاولات الكتابة سترفضها قاعدة البيانات حتى لو كانت صفة
`kaziet_admin` مضبوطة فعلاً في `auth.users`، لأن الـ JWT القديم في الجلسة
لا يحملها بعد.

**التأخر يعمل في الاتجاهين، وسحب الصفة أخطر مما يبدو.** إن أزلت
`kaziet_admin` من مستخدم كان مديراً، تبقى جلسته الحالية — وJWT الصادر لها
سابقاً — تحمل الصفة القديمة وتسمح له بالكتابة فعلياً حتى **ينتهي عمر
الـ access token نفسه** (عادة ساعة واحدة افتراضياً في Supabase). هذا
الرمز صالح ويُقبل من PostgREST/RLS طوال مدته بصرف النظر عمّا تفعله
بحساب المستخدم بعد إصداره.

**تنبيه مهم:** إنهاء الجلسة من جهة الخادم (`supabaseAdmin.auth.admin.signOut`)
أو حذف/تدوير الـ refresh tokens **لا يُبطل الـ access token الحالي
فوراً** — وثائق Supabase توضح أن access token يبقى صالحاً حتى انتهاء
مدته حتى بعد `signOut`؛ ما يفعله السحب أو `signOut` هو منع **تجديد**
الرمز لاحقاً فقط (فلا يحصل المستخدم على access token جديد بعد انتهاء
الحالي). فلا تصف `signOut` كحل لإبطال فوري — هو ليس كذلك.

إن كانت الحاجة الفعلية إبطالاً فورياً حقيقياً (مثلاً مدير أُقيل ويُشتبه
بسوء استخدام)، الاعتماد على `app_metadata` وحده لا يكفي أياً كانت
الخطوات المتخذة على الجلسة، لأن السياسات تتحقق من الـ claim المحفوظ في
JWT القديم نفسه لا من حالة الحساب الحالية. الحل الوحيد الفعلي هو أحد
اثنين: (أ) تقييد النافذة الزمنية بتقصير مدة الـ access token في إعدادات
المشروع مسبقاً، أو (ب) جعل سياسة الكتابة تتحقق من مصدر حيّ بدل الاكتفاء
بالـ claim — كأن تستعلم دالة `kaziet_is_admin()` من جدول مدراء حقيقي
(`SELECT ... FROM public.admins WHERE user_id = auth.uid()`) بدل قراءة
`app_metadata` من الـ JWT، فتنعكس عليها كل عملية إقالة فوراً في الطلب
التالي بلا انتظار انتهاء الرمز. هذا خارج نطاق هذا الترقيع الحالي ويحتاج
تصميماً منفصلاً إن كان الإبطال الفوري متطلباً فعلياً للمشروع.

## 4. خطة الاختبار (بعد التطبيق)

**تحذير مهم: لا تختبر هذا من SQL Editor بمحاولة `UPDATE`/`INSERT` مباشرة
على الجداول.** SQL Editor في Supabase يتصل بصلاحية `postgres`
(superuser)، وRLS لا يُطبَّق على الأدوار التي تملك خاصية `BYPASSRLS` —
والـ superuser منها دائماً. أي كتابة تنجح هناك لا تثبت شيئاً عن سلوك
RLS الفعلي؛ ينجح حتى لو كانت السياسات معطوبة تماماً. الإثبات الحقيقي
لازم يمرّ عبر الدور الفعلي الذي يستخدمه العميل (`anon` أو
`authenticated`)، وهذا يحتاج أحد مسارين:

**أ. جلسات عميل منفصلة فعلية (الأدق والأوصى به):**
1. **مجهول** — افتح `admin.html` (أو استدعِ REST API عبر `curl`
   بمفتاح anon من `config.js`) بلا تسجيل دخول، وحاول قراءة/كتابة.
2. **مستخدم عادي** — أنشئ حساباً تجريبياً بلا صفة `kaziet_admin`، سجّل
   دخوله فعلياً من `admin.html`، وحاول تعديل حالة الوقود أو رفع صورة.
3. **مدير حقيقي** — بعد تحديث الجلسة كما في القسم 3، كرّر نفس المحاولات
   من نفس الواجهة.

**ب. محاكاة دور/JWT صحيحة داخل SQL Editor (إن تعذّر تسجيل الدخول
الفعلي)**، لا محاولة `UPDATE` كـ postgres:
```sql
SET ROLE authenticated;  -- ليس postgres
SET request.jwt.claims = '{"app_metadata": {}}';  -- مستخدم عادي
-- أو '{"app_metadata": {"kaziet_admin": true}}' لمحاكاة المدير
UPDATE site_content SET value = value WHERE id = 'fuel_status';
RESET ROLE;
```
التحقق الصحيح هنا هو عدد الصفوف المتأثرة (`UPDATE 0` لمستخدم عادي،
`UPDATE 1` للمدير)، لا مجرّد غياب رسالة خطأ.

| الحالة | العملية | النتيجة المتوقعة |
|---|---|---|
| مجهول (anon key، بلا جلسة) | قراءة `site_content` | تنجح (قراءة عامة مقصودة) |
| مجهول (anon key، بلا جلسة) | تحديث/إدراج في `site_content` أو رفع صورة | **تُرفض** |
| مستخدم عادي مسجّل دخوله (بلا `kaziet_admin`) | تحديث/إدراج في `site_content` | **تُرفض** — هذا هو الفرق الجوهري عن السلوك القديم |
| مستخدم عادي مسجّل دخوله (بلا `kaziet_admin`) | رفع صورة إلى `media` | **تُرفض** |
| المدير الحقيقي (بعد تحديث الجلسة) | تحديث حالة الوقود، رفع صورة، حذف الصورة من `media` | تنجح جميعها من `admin.html` كما في السابق |
| **أي دور بلا استثناء (مجهول/مستخدم عادي/المدير نفسه)** | `DELETE` على صفوف `site_content` | **تُرفض دائماً** — لا توجد سياسة `DELETE` على `site_content` أصلاً بعد هذا الترقيع (راجع "انحراف مكتشف" أعلاه)؛ الواجهة لا تحتاجها أساساً |

**ج. مراجعة `pg_policies` فعلياً قبل التطبيق وبعده (هذا استعلام قراءة
فقط على الكتالوج، لا كتابة، فلا مشكلة في تشغيله كـ postgres):**
```sql
SELECT schemaname, tablename, policyname, cmd, roles, qual, with_check
FROM pg_policies
WHERE tablename IN ('site_content', 'objects')
ORDER BY tablename, policyname;
```
**القائمة "قبل" (8 سياسات؛ كانت الحالة الفعلية على مشروعكم الحي قبل
التطبيق، بفحص `pg_policies` مباشر بتاريخ تلك المراجعة — الآن تاريخية،
فالتطبيق حصل فعلاً، انظر "حالة التطبيق على المشروع الحي" أعلاه):**

| tablename | policyname | cmd |
|---|---|---|
| site_content | Allow public read | SELECT |
| site_content | Allow admin update | UPDATE |
| site_content | Allow admin insert | INSERT |
| site_content | **Allow admin delete** | DELETE |
| objects | Public Image Access | SELECT |
| objects | Admin Image Upload | INSERT |
| objects | Admin Image Update | UPDATE |
| objects | Admin Image Delete | DELETE |

**القائمة "بعد" (7 سياسات؛ كانت متوقَّعة ومختبَرة محلياً بمحاكاة هذا
الوضع تحديداً على Postgres 16 قبل التطبيق — والآن **مؤكَّدة فعلياً على
الحي أيضاً**: تطابق حرفياً نتيجة `pg_policies` التي نقلها المالك بعد
التطبيق، الموثَّقة في "حالة التطبيق على المشروع الحي" أعلاه):**

| tablename | policyname | cmd |
|---|---|---|
| site_content | Allow public read | SELECT |
| site_content | Admin write update | UPDATE |
| site_content | Admin write insert | INSERT |
| objects | Public Image Access | SELECT |
| objects | Admin Image Upload | INSERT |
| objects | Admin Image Update | UPDATE |
| objects | Admin Image Delete | DELETE |

لاحظوا: **لا توجد سياسة `DELETE` على `site_content` في القائمة "بعد"
إطلاقاً** — هذا مقصود، لا سهو (راجع قسم "انحراف مكتشف" أعلاه).

الترقيع كان يُسقط أسماء سياسات بعينها (`Allow admin update`، `Allow
admin insert`، `Allow admin delete`، `Admin Image Upload/Update/Delete`)،
وهذا ما أكّدته فعلياً نتيجة `pg_policies` بعد التطبيق (القائمة "بعد"
أعلاه، 7 سياسات مطابقة تماماً). هذا الاستعلام سليم أيضاً لالتقاط أي
سياسة كتابة أخرى بأسماء مختلفة لم يُتوقَّعها هذا الترقيع (كما حدث
سابقاً مع `Allow admin delete` قبل اكتشافها) — ونتيجة مساعد المالك
المنقولة أعلاه (7 سياسات بالضبط، بلا أي `auth.role() = 'authenticated'`
وحده) لا تُظهر أي سياسة إضافية من هذا النوع. **هذا لا يغلق التحقق
بالكامل**: يبقى اختبار الواجهة والجلسة/JWT الفعلي (أدناه) غير مكتمل.
من أراد تحققاً ثانياً مستقلاً في `pg_policies` نفسها (لا نقلاً عن هذه
الوثيقة) يمكنه إعادة تشغيل نفس الاستعلام مباشرة.

**ما بقي غير محقَّق (يحتاج مشروع Supabase حيّاً تحديداً، ولم يُغطَّه
فحص `pg_policies`):**
- هل مشروعكم الحي يفعّل "تسجيل مجهول" (Anonymous Sign-ins) فعلاً؟ لم
  يُتحقق من هذا الإعداد تحديداً — الترقيع يسدّ الثغرة بصرف النظر عن
  الجواب، لكن تأكيد الإعداد نفسه لم يُطلب ولم يُنقَل.
- اختبار دخول المالك الفعلي من `admin.html` وتدفّق الكتابة الكامل
  (الجلسة، JWT المُصدَر، طلب PostgREST) — راجع "حالة التطبيق على
  المشروع الحي" أعلاه.
- سلوك PostgREST/GoTrue الفعلي (رؤوس الطلبات، صياغة أخطاء الرفض في
  الواجهة) على نسخة Supabase المُستضافة تحديداً.

## 5. التراجع (Rollback)

**لا يوجد "تراجع" آمن بإعادة فتح الكتابة لكل `auth.role() = 'authenticated'`.**
هذا ليس تراجعاً بل عودة إلى الثغرة نفسها التي يسدّها هذا الـ PR، فهو غير
موجود هنا عمداً ولا يُنصح بكتابته يدوياً. إن تعطّلت لوحة الإدارة بعد
التطبيق، المسار الصحيح تشخيصٌ ثم إصلاحٌ يبقي RLS مقيّداً بصفة المدير:

### أ. الحالة الشائعة: فشل دخول المدير نفسه

هذا يكاد يكون دائماً JWT قديم أو `app_metadata` غير مضبوطة، لا عطباً في
السياسات:

1. تحقّق من `app_metadata` فعلياً (استعلام القسم 1.3) — هل `kaziet_admin`
   فعلاً `true` لحساب المدير الذي يحاول الدخول؟ إن لم يكن، صحّحه بنفس
   استعلام `UPDATE` أو عبر Admin API.
2. جدِّد الجلسة: تسجيل خروج ثم دخول من `admin.html`، أو
   `client.auth.refreshSession()` (القسم 3). لا تحاول أي تعديل على
   السياسات قبل تجربة هذا.
3. تحقّق من عدم انتهاء صلاحية حساب المدير أو تعطيله من Authentication →
   Users.

### ب. طوارئ حقيقية فقط (تعطّل عام لا يُفسَّر بخطوة أ)

إن استمر التعطّل بعد التأكد من app_metadata وتجديد الجلسة، فالاحتمال
الأرجح خلل في السياسات نفسها أو في المخطط، لا في صفة مستخدم واحد.
**العلاج الافتراضي هو إصلاحٌ للأمام مع إبقاء RLS مقيّداً بصفة المدير
طوال الوقت**، لا استعادة نسخة قديمة:

1. شخّص الخلل عبر `pg_policies` (استعلام القسم 4.ج) — قارن السياسات
   الفعلية الحالية بما يفترض أن ينتجه `supabase_setup.sql`/ملف
   الترقيع، وحدّد أين الانحراف.
2. أعد تطبيق تعريف السياسة الصحيح تحديداً (`DROP POLICY` ثم
   `CREATE POLICY` بنفس صياغة الترقيع لتلك السياسة وحدها) بدل أي
   تعديل شامل أو استعادة.
3. تحقّق فوراً بعد الإصلاح بمصفوفة الصلاحيات (القسم 4) قبل إعلان
   الحادثة منتهية.

**تحذير: لا تستعيدوا نسخة قديمة (PITR أو نسخة احتياطية) من قبل تطبيق
الترقيع إلى مشروع متاح للعامة.** هذا يُعيد سياسات الكتابة القديمة
المتساهلة نفسها فعلياً، ويُفقد أيضاً أي بيانات كُتبت بعد وقت اللقطة
(حالة الوقود، الصور).

**لا يوجد إجراء عزل بسيط يجعل هذه الاستعادة آمنة من هذا الملف وحده،
ولا نقترح واحداً هنا:**
- **حجب `admin.html` أو حذف مفتاحه لا يمنع أحداً من مناداة Supabase
  API مباشرة** (عبر `curl` أو أي عميل آخر بمفتاح anon العام في
  `config.js`) — الحماية الوحيدة الحقيقية هي RLS على القاعدة، لا
  إخفاء صفحة في الواجهة.
- **إضافة سياسة RLS جديدة بـ `USING (false)` لا تُعطّل السياسات
  المتساهلة القديمة القائمة أصلاً.** سياسات RLS من نوع PERMISSIVE
  (النوع الافتراضي، وكل سياسات هذا المشروع منه) **تُجمع فيما بينها
  بـ `OR`**: وجود سياسة قديمة تسمح بالكتابة يكفي وحده للسماح بها، بصرف
  النظر عن أي سياسة `false` إضافية تُضاف بجانبها. عزل فعلي حقيقي
  يحتاج إما إسقاط كل سياسات الكتابة المتساهلة صراحة (وهذا ما يفعله ملف
  الترقيع أصلاً — فلا داعي لخطوة عزل منفصلة قبله) أو تعطيل RLS بالكامل
  مؤقتاً على مستوى القاعدة (`ALTER TABLE ... DISABLE ROW LEVEL
  SECURITY`، وهو أخطر لا أبسط، ويفتح الجدول بلا أي قيد إطلاقاً أثناء
  التعطيل).

إن كان التلف في البيانات نفسها (لا في السياسات) يستدعي فعلاً استعادة
لقطة قديمة، فهذا قرار عملياتي عالي الخطورة يتجاوز نطاق ملف SQL أو
توثيق وحدهما: يحتاج **خطة عزل حقيقية مستقلة يضعها ويتحقق منها مسؤول
مشروع Supabase نفسه** (لا إجراء عام مكتوب هنا يُطبَّق آلياً)، ثم — بعد
الاستعادة وقبل إعادة إتاحة المشروع للعامة مطلقاً — إعادة تطبيق ملف
الترقيع من جديد والتحقق فعلياً من `pg_policies` (القسم 4.ج) ومصفوفة
الصلاحيات (القسم 4) قبل رفع أي عزل.

### ج. المبدأ العام

الإصلاح الصحيح دائماً هو تصحيح تعيين `kaziet_admin` وتجديد الـ JWT، لا
تخفيف RLS. أي اقتراح "افتح الكتابة مؤقتاً وأغلقها لاحقاً" هو بالضبط
النمط الذي أنتج الثغرة الأصلية في هذا المشروع (`fix_admin.sql` القديم).
